# Local export of hackathon output (team deliverables, shared uploads,
# sample data) pulled off the event VM after it ended — see
# vibecoding-smdg-80/README.md. Not part of the infra itself, never
# committed. `downloads/` is the working name used while building the
# export before it's renamed.
downloads/
vibecoding-smdg-80/
